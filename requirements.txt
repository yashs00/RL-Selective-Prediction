# Curated, pinned direct dependencies (per project plan §8).
# For an exact, fully-reproducible environment (all transitive deps too),
# use requirements-freeze.txt instead: `pip install -r requirements-freeze.txt`.
#
# NOTE: the project plan specifies python 3.11. This environment only had
# python 3.14 available, so that is what was used and verified end-to-end
# (see PROJECT_STATUS.md). If you provision python 3.11 instead, recreate
# the venv and re-run `pip install -r requirements.txt`; everything here is
# pinned to versions that also publish 3.11 wheels.

numpy==2.5.2
pandas==3.0.5
pyarrow==25.0.1
scipy==1.18.1
scikit-learn==1.9.0
lightgbm==4.7.0
xgboost==3.4.1
torch==2.14.0
openml==0.15.1
hydra-core==1.3.6
omegaconf==2.3.1
matplotlib==3.11.1
statsmodels==0.15.0
pytest==9.1.1
